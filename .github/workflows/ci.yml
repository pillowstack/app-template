name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  packages: read

jobs:
  ci:
    name: CI Job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci
        env:
          NODE_AUTH_TOKEN: ${{ secrets.PKG_GH_READ }}

      - name: Lint code
        run: npm run lint

      - name: Prettier check
        run: npm run format:check

      - name: Build (uploads sourcemaps via Sentry Vite Plugin)
        run: npm run build
        env:
          VITE_APPLE_TEAM_ID: ${{ vars.APPLE_TEAM_ID }}
          VITE_APPLE_BUNDLE_ID: ${{ vars.APPLE_BUNDLE_ID }}
          VITE_APPLE_SERVICE_ID: ${{ vars.APPLE_SERVICE_ID }}
          VITE_APPLE_REDIRECT_URI: ${{ vars.APPLE_REDIRECT_URI }}
          VITE_APPLE_SCOPE: ${{ vars.APPLE_SCOPE }}
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: ${{ vars.SENTRY_ORG }}
          SENTRY_PROJECT: ${{ vars.SENTRY_PROJECT }}
          SENTRY_RELEASE: pr-${{ github.event.number }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}

      - name: Unit Tests
        run: npm run test:unit

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: e2e Test
        run: npm run test:e2e

      - name: Deploy preview to Netlify
        uses: netlify/actions/cli@master
        with:
          args: deploy --dir=dist --site ${{ secrets.NETLIFY_SITE_ID }} --auth ${{ secrets.NETLIFY_AUTH_TOKEN }}
