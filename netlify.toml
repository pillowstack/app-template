[build]
  command = "npm run build"
  publish = "dist"
  ignore = "if [ \"$CONTEXT\" = \"deploy-preview\" ]; then exit 1; else echo Skipping $CONTEXT; exit 0; fi"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
